// Leaderboard backend URL.
//
// Leave this EMPTY to keep the leaderboard hidden (the game works fully
// without it). To switch the leaderboard on, deploy the Cloudflare Worker in
// server/ (see server/README.md), then paste its URL here, e.g.:
//
//   window.LEADERBOARD_API = "https://mapquiz-api.yourname.workers.dev";
//
window.LEADERBOARD_API = "https://mapquiz-api.dan-da4.workers.dev";
